# Django Settings
SECRET_KEY=your-super-secret-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,[::1],api.autoparts.ke

# PostgreSQL Database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=autoparts_kenya
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis & Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# JWT Tokens
JWT_ACCESS_TOKEN_LIFETIME=24h  # Hours
JWT_REFRESH_TOKEN_LIFETIME=7d  # Days

# M-Pesa / Daraja API (Sandbox credentials for testing)
MPESA_CONSUMER_KEY=your-consumer-key-from-daraja
MPESA_CONSUMER_SECRET=your-consumer-secret-from-daraja
MPESA_BUSINESS_SHORTCODE=174379  # Test shortcode (sandbox)
MPESA_PASSKEY=bfb279f9aa9bdbcf158e97dd1a503b6015d86d3b50c3cc1f  # Test passkey (sandbox)
MPESA_ENVIRONMENT=sandbox  # Change to 'production' when ready
MPESA_CALLBACK_URL=https://api.autoparts.ke/api/v1/payments/mpesa-callback/

# M-Pesa STK Push Settings
MPESA_TIMEOUT=30  # Seconds before STK timeout

# Business Information
STORE_NAME=AutoParts Kenya
STORE_PHONE=+254712345678
STORE_EMAIL=hello@autoparts.ke
STORE_CURRENCY=KSh
STORE_CURRENCY_CODE=KES

# Delivery Configuration
DELIVERY_BASE_RATE=200  # KSh
DELIVERY_MAX_DAYS=3
DELIVERY_BASE_CITY=Nairobi

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,https://autoparts.ke

# Logging
DJ_LOG_LEVEL=INFO

# Email Configuration (optional - for order confirmations, notifications)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# AWS S3 (optional - for media storage in production)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=eu-west-1

# Sentry (optional - error tracking)
SENTRY_DSN=
